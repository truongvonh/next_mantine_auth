import React from 'react';

const NotFoundComponent = () => {
	return (
		<div>
			<h3>
				<p>This is the not found component</p>
			</h3>
		</div>
	);
};

export default React.memo(NotFoundComponent);
